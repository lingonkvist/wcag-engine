/**
 * wcag-engine, a WCAG 2.2 rules engine for design values.
 *
 * @module wcag-engine
 */

// Placeholder export so the smoke test passes.
// Real API will replace this.
export const version = '0.0.1'
